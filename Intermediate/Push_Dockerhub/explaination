First of all I need to login to docker hub.
//Then save the image by finding the container ID,
    docker container commit c16378f943fe rhel-httpd:latest//

//Then push the image to the registry using Image ID,
    docker image push registry-host:5000/myadmin/rhel-httpd:latest//

Then you can counter check it by docker image ls